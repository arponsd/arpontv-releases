# ArponTV — Releases

Public release artifacts for the **ArponTV** Android app (source is private).

- `update.json` — the version manifest the app polls on launch.
- APK builds are attached to this repo's **Releases**.

Both are published automatically by the app repo's release workflow.
